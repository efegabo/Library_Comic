  <div class="serc">
   
    <form action="/search" method="GET">
     <div class="group">      
      <input type="text" name="query" required>
      <span class="highlight"></span>
      <span class="bar"></span>
      <label>Buscar</label>
    </div>

    <button class="btn-se" type="submit"><i class="bi bi-search"></i></button>
  </form>
    </div>
<div class="bdy">
   <!--{{>generos title="Generos"}}-->
    
   <div class="card_grip">
     {{#if portadas.length}}
     {{#each portadas}}

<div class="card">
    
       <a href="/info/cap/{{_id}}"><img src="{{imageUrl}}" alt=""></a>  
       <div class="card_cont_title">
          <a href="/info/cap/{{_id}}" class="title_card"><strong><p>{{title}}</p></strong> </a>  
       </div>
</div>
 {{/each}}
 {{else}}
 <p>No se encontraron mangas.</p>
    {{/if}}
</div>
</div>
 {{>carrusel}}
 
 